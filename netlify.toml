# Netlify configuration for portfolio site

[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Form settings
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
